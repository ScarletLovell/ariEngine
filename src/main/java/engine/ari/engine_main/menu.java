package engine.ari.engine_main;

import engine.ari.engine_main.rendering.Textures;

import java.util.HashMap;

public class menu {
    /*private engine.ari.engine_main.rendering.Textures Textures = new Textures();
    private Console Console = new Console();
    private Boolean js = false;
    public void applyJS(Boolean b, Integer code) {
        if(code == 768132761)
            js = b;
    }

    private static HashMap<String, HashMap> menus = new HashMap<>();
    public void createNewMenu(String menuName) {
        menus.put(menuName, new HashMap());
    }

    private interface buttons {
        HashMap<String, Button> button = new HashMap<>();
        HashMap<Button, Integer> button_x = new HashMap<>();
        HashMap<Button, Integer> button_y = new HashMap<>();
    }
    public void createNewButton(String buttonName, Integer texture_id, int x, int y) {
        Texture texture = Textures.get(texture_id).getTexture();
        if(texture == null) {
            engine.ari.engine_main.Console.log("Could not find texture by id: " + texture_id);
            return;
        }
        //Button
        Button button = new Button();
        buttons.button.put(buttonName, button);
        buttons.button_x.put(button, x);
        buttons.button_y.put(button, y);
    }
    public void destroyButton(String buttonName) {
        if(!buttons.button.containsKey(buttonName)) {
            engine.ari.engine_main.Console.log("Could not find the button by the name: " + buttonName);
        }
    }*/
}
